<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="DeviceCovers Premium Login - Secure Access to Your Insurance Dashboard">
  <title>Premium Login - DeviceCovers</title>
  <link rel="stylesheet" href="/css/premium.css">
  <style>
    .auth-container {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 0;
      height: 100vh;
      background: var(--bg-primary);
    }

    .auth-branding {
      background: var(--gradient-primary);
      color: white;
      padding: var(--space-12);
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      position: relative;
      overflow: hidden;
    }

    .auth-branding::before {
      content: '';
      position: absolute;
      top: -50%;
      right: -50%;
      width: 200%;
      height: 200%;
      background: radial-gradient(circle, rgba(255, 255, 255, 0.1) 0%, transparent 70%);
      animation: float 20s ease-in-out infinite;
    }

    .brand-content {
      position: relative;
      z-index: 1;
      text-align: center;
    }

    .brand-icon {
      font-size: 5rem;
      margin-bottom: var(--space-4);
      filter: drop-shadow(0 0 20px rgba(255, 255, 255, 0.3));
    }

    .brand-content h1 {
      font-size: 3rem;
      font-weight: var(--font-extrabold);
      margin-bottom: var(--space-2);
    }

    .brand-tagline {
      font-size: var(--text-xl);
      opacity: 0.9;
      margin-bottom: var(--space-12);
      font-weight: var(--font-light);
    }

    .brand-features {
      display: flex;
      flex-direction: column;
      gap: var(--space-4);
      margin-bottom: var(--space-12);
    }

    .brand-feature {
      display: flex;
      align-items: center;
      gap: var(--space-3);
      font-size: var(--text-lg);
    }

    .feature-icon {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      width: 30px;
      height: 30px;
      background: rgba(255, 255, 255, 0.2);
      border-radius: var(--rounded-full);
      font-weight: bold;
    }

    .brand-stats {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: var(--space-8);
      margin-top: var(--space-12);
      padding-top: var(--space-12);
      border-top: 1px solid rgba(255, 255, 255, 0.2);
    }

    .stat strong {
      display: block;
      font-size: var(--text-2xl);
      margin-bottom: var(--space-1);
    }

    .stat small {
      opacity: 0.8;
      font-size: var(--text-sm);
    }

    .auth-form-container {
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      padding: var(--space-12);
    }

    .auth-form-wrapper {
      width: 100%;
      max-width: 400px;
    }

    .auth-form-title {
      font-size: var(--text-3xl);
      font-weight: var(--font-extrabold);
      color: var(--primary);
      margin-bottom: var(--space-2);
    }

    .auth-form-subtitle {
      color: var(--text-secondary);
      margin-bottom: var(--space-8);
      font-size: var(--text-sm);
    }

    .form-group {
      margin-bottom: var(--space-6);
    }

    .form-group label {
      display: block;
      font-weight: var(--font-semibold);
      margin-bottom: var(--space-2);
      color: var(--text-primary);
    }

    .form-group input {
      width: 100%;
      padding: var(--space-3) var(--space-4);
      border: 2px solid var(--border);
      border-radius: var(--rounded-md);
      font-size: inherit;
      transition: all var(--transition-base);
    }

    .form-group input:focus {
      outline: none;
      border-color: var(--secondary);
      box-shadow: 0 0 0 3px var(--secondary-light);
    }

    .auth-links {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: var(--space-6);
      font-size: var(--text-sm);
    }

    .auth-links a {
      color: var(--secondary);
      text-decoration: none;
      transition: color var(--transition-base);
    }

    .auth-links a:hover {
      color: var(--secondary-dark);
    }

    .checkbox-group {
      display: flex;
      align-items: center;
      gap: var(--space-2);
    }

    .checkbox-group input[type="checkbox"] {
      width: auto;
      margin: 0;
    }

    .btn-login {
      width: 100%;
      padding: var(--space-3) var(--space-4);
      background: var(--gradient-primary);
      color: white;
      border: none;
      border-radius: var(--rounded-md);
      font-weight: var(--font-bold);
      font-size: var(--text-base);
      cursor: pointer;
      transition: all var(--transition-base);
      box-shadow: var(--shadow-lg);
    }

    .btn-login:hover {
      transform: translateY(-2px);
      box-shadow: var(--shadow-xl), var(--shadow-glow);
    }

    .divider {
      display: flex;
      align-items: center;
      gap: var(--space-4);
      margin: var(--space-6) 0;
      color: var(--text-tertiary);
      font-size: var(--text-sm);
    }

    .divider::before,
    .divider::after {
      content: '';
      flex: 1;
      height: 1px;
      background: var(--border);
    }

    .social-login {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: var(--space-4);
      margin-bottom: var(--space-6);
    }

    .social-btn {
      padding: var(--space-3);
      border: 2px solid var(--border);
      background: var(--bg-primary);
      border-radius: var(--rounded-md);
      cursor: pointer;
      font-size: var(--text-lg);
      transition: all var(--transition-base);
    }

    .social-btn:hover {
      border-color: var(--primary);
      background: var(--primary-light);
    }

    .signup-link {
      text-align: center;
      padding-top: var(--space-6);
      border-top: 1px solid var(--border);
      font-size: var(--text-sm);
      color: var(--text-secondary);
    }

    .signup-link a {
      color: var(--primary);
      font-weight: var(--font-bold);
      text-decoration: none;
      transition: color var(--transition-base);
    }

    .signup-link a:hover {
      color: var(--secondary);
    }

    @media (max-width: 768px) {
      .auth-container {
        grid-template-columns: 1fr;
      }

      .auth-branding {
        padding: var(--space-8);
        min-height: 200px;
      }

      .brand-icon {
        font-size: 3rem;
      }

      .brand-content h1 {
        font-size: 2rem;
      }

      .brand-stats {
        margin-top: var(--space-8);
        padding-top: var(--space-8);
      }

      .auth-form-container {
        padding: var(--space-6);
      }
    }

    @media (max-width: 480px) {
      .auth-branding {
        display: none;
      }

      .auth-container {
        height: auto;
        min-height: 100vh;
      }

      .auth-form-container {
        padding: var(--space-4);
      }

      .auth-form-wrapper {
        max-width: 100%;
      }
    }
  </style>
</head>
<body>
<nav class="navbar">
  <div class="navbar-container">
    <a href="/" class="logo">
      <span class="logo-icon">🛡️</span>
      <span>DeviceCovers</span>
    </a>
    <ul class="nav-menu">
      <li><a href="/">Home</a></li>
      <li><a href="/register" class="btn btn-primary">Register</a></li>
    </ul>
  </div>
</nav>

<div class="page-content">
  <div class="auth-container">
    <!-- Left Side - Branding -->
    <div class="auth-branding">
      <div class="brand-content">
        <div class="brand-icon">🛡️</div>
        <h1>DeviceCovers</h1>
        <p class="brand-tagline">Enterprise-Grade Premium Device Protection</p>
        
        <div class="brand-features">
          <div class="brand-feature">
            <span class="feature-icon">✓</span>
            <span>Fast & Secure Authentication</span>
          </div>
          <div class="brand-feature">
            <span class="feature-icon">✓</span>
            <span>Premium Device Coverage</span>
          </div>
          <div class="brand-feature">
            <span class="feature-icon">✓</span>
            <span>24/7 Billion-Dollar Support</span>
          </div>
        </div>

        <div class="brand-stats">
          <div class="stat">
            <strong>50K+</strong>
            <small>Active Users</small>
          </div>
          <div class="stat">
            <strong>ZMW 100B+</strong>
            <small>Protected Value</small>
          </div>
        </div>
      </div>
    </div>

    <!-- Right Side - Form -->
    <div class="auth-form-container">
      <div class="auth-form-wrapper">
        <div>
          <h2 class="auth-form-title">Welcome Back</h2>
          <p class="auth-form-subtitle">Sign in to your premium insurance dashboard</p>
        </div>

        <form id="loginForm" class="modern-form">
          <div class="form-group">
            <label for="email">Email Address</label>
            <input 
              type="email" 
              id="email" 
              name="email" 
              placeholder="you@example.com"
              required 
            />
          </div>

          <div class="form-group">
            <label for="password">Password</label>
            <input 
              type="password" 
              id="password" 
              name="password" 
              placeholder="••••••••"
              required 
            />
          </div>

            <div class="form-options">
            <label class="checkbox-group">
              <input type="checkbox" name="remember" />
              <span>Remember me</span>
            </label>
            <a href="#">Forgot password?</a>
          </div>

          <button type="submit" class="btn-login">
            🔓 Sign In Securely
          </button>
        </form>

        <div class="divider">
          Don't have an account?
        </div>

        <a href="/register" class="btn btn-secondary" style="width: 100%; justify-content: center;">
          Create Premium Account
        </a>

        <p class="signup-link" style="margin-top: var(--space-6);">
          By signing in, you agree to our <a href="#">Terms of Service</a> and <a href="#">Privacy Policy</a>
        </p>
        </div>
      </div>
    </div>
  </div>
</div>

<footer class="footer">
  <div class="footer-container">
    <div class="footer-section">
      <h4>Product</h4>
      <ul>
        <li><a href="/">Home</a></li>
        <li><a href="/#features">Features</a></li>
        <li><a href="/">Plans</a></li>
        <li><a href="#">Blog</a></li>
      </ul>
    </div>
    <div class="footer-section">
      <h4>Support</h4>
      <ul>
        <li><a href="#">Help Center</a></li>
        <li><a href="#">Contact Us</a></li>
        <li><a href="#">FAQ</a></li>
        <li><a href="#">Status</a></li>
      </ul>
    </div>
    <div class="footer-section">
      <h4>Legal</h4>
      <ul>
        <li><a href="#">Privacy Policy</a></li>
        <li><a href="#">Terms of Service</a></li>
        <li><a href="#">Security</a></li>
        <li><a href="#">Compliance</a></li>
      </ul>
    </div>
    <div class="footer-section">
      <h4>Connect</h4>
      <ul>
        <li><a href="#">Twitter</a></li>
        <li><a href="#">LinkedIn</a></li>
        <li><a href="#">Facebook</a></li>
        <li><a href="#">Instagram</a></li>
      </ul>
    </div>
  </div>
  <div class="footer-bottom">
    <p>&copy; 2026 DeviceCovers. Premium Device Insurance. All rights reserved.</p>
  </div>
</footer>

<script>
  document.getElementById('loginForm').addEventListener('submit', async (e) => {
    e.preventDefault();
    const formData = new FormData(e.target);
    const data = Object.fromEntries(formData);

    try {
      const button = e.target.querySelector('button[type="submit"]');
      button.disabled = true;
      button.textContent = '⏳ Signing in...';

      const response = await fetch('/api/auth/login', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(data),
      });

      const result = await response.json();
      if (response.ok) {
        localStorage.setItem('token', result.token);
        window.location.href = '/dashboard';
      } else {
        alert(result.message || 'Login failed');
        button.disabled = false;
        button.textContent = '🔓 Sign In Securely';
      }
    } catch (error) {
      alert('Login failed: ' + error.message);
      const button = e.target.querySelector('button[type="submit"]');
      button.disabled = false;
      button.textContent = '🔓 Sign In Securely';
    }
  });
</script>

<style>
  /* Auth Page Layout */
  .auth-page {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, var(--bg-secondary) 0%, var(--bg-tertiary) 100%);
    padding: var(--space-4);
  }

  .auth-container {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-12);
    width: 100%;
    max-width: 1200px;
    background: var(--bg-primary);
    border-radius: 20px;
    overflow: hidden;
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.15);
  }

  /* Branding Section */
  .auth-branding {
    background: var(--gradient-primary);
    padding: var(--space-12);
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    overflow: hidden;
  }

  .auth-branding::before {
    content: '';
    position: absolute;
    top: -50%;
    right: -20%;
    width: 400px;
    height: 400px;
    background: rgba(255, 255, 255, 0.1);
    border-radius: 50%;
  }

  .brand-content {
    position: relative;
    z-index: 1;
    color: white;
    text-align: center;
  }

  .brand-icon {
    font-size: 4rem;
    margin-bottom: var(--space-4);
    display: block;
  }

  .brand-content h1 {
    font-size: 2rem;
    margin-bottom: var(--space-2);
    font-weight: 800;
  }

  .brand-tagline {
    font-size: 1.1rem;
    opacity: 0.95;
    margin-bottom: var(--space-8);
  }

  .brand-features {
    display: flex;
    flex-direction: column;
    gap: var(--space-4);
    margin-bottom: var(--space-8);
  }

  .brand-feature {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    font-size: 1rem;
  }

  .feature-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 28px;
    height: 28px;
    background: rgba(255, 255, 255, 0.2);
    border-radius: 50%;
    font-weight: bold;
  }

  .brand-stats {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-6);
    margin-top: var(--space-8);
    padding-top: var(--space-8);
    border-top: 1px solid rgba(255, 255, 255, 0.2);
  }

  .stat {
    text-align: center;
  }

  .stat strong {
    display: block;
    font-size: 1.5rem;
    margin-bottom: var(--space-1);
  }

  .stat small {
    font-size: 0.9rem;
    opacity: 0.9;
  }

  /* Form Section */
  .auth-form-container {
    padding: var(--space-12);
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .auth-form-wrapper {
    width: 100%;
    max-width: 400px;
  }

  .form-header {
    margin-bottom: var(--space-8);
  }

  .form-header h2 {
    font-size: 2rem;
    color: var(--text-primary);
    margin-bottom: var(--space-2);
  }

  .form-header p {
    color: var(--text-secondary);
    font-size: 1rem;
  }

  .modern-form {
    display: flex;
    flex-direction: column;
    gap: var(--space-6);
  }

  .modern-form .form-group {
    position: relative;
    margin-bottom: 0;
  }

  .modern-form .form-group label {
    display: block;
    margin-bottom: var(--space-2);
    font-weight: 600;
    color: var(--text-primary);
  }

  .modern-form .form-group input {
    width: 100%;
    padding: var(--space-3) var(--space-4) var(--space-3) var(--space-4);
    padding-right: 40px;
    border: 2px solid var(--border);
    border-radius: 8px;
    font-size: 1rem;
    transition: all 0.3s ease;
    background: var(--bg-primary);
  }

  .modern-form .form-group input:focus {
    outline: none;
    border-color: var(--primary);
    box-shadow: 0 0 0 4px rgba(0, 102, 255, 0.1);
    background: var(--bg-primary);
  }

  .form-icon {
    position: absolute;
    right: var(--space-3);
    top: 50%;
    transform: translateY(-50%);
    margin-top: var(--space-5);
    font-size: 1.2rem;
    pointer-events: none;
  }

  .form-options {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: var(--space-4);
    margin: var(--space-4) 0;
  }

  .checkbox-group {
    display: flex;
    align-items: center;
    gap: var(--space-2);
    cursor: pointer;
    user-select: none;
  }

  .checkbox-group input[type="checkbox"] {
    width: 18px;
    height: 18px;
    cursor: pointer;
    accent-color: var(--primary);
  }

  .checkbox-group span {
    font-size: 0.95rem;
    color: var(--text-secondary);
  }

  .forgot-link {
    color: var(--primary);
    text-decoration: none;
    font-weight: 500;
    transition: all 0.3s ease;
  }

  .forgot-link:hover {
    color: var(--primary-dark);
    text-decoration: underline;
  }

  .auth-btn {
    width: 100%;
    padding: var(--space-4) var(--space-6);
    font-size: 1.05rem;
    font-weight: 600;
    border: none;
    cursor: pointer;
  }

  .auth-divider {
    text-align: center;
    margin: var(--space-8) 0 var(--space-6) 0;
    color: var(--text-tertiary);
    font-size: 0.95rem;
  }

  .auth-footer {
    text-align: center;
    margin-top: var(--space-6);
    font-size: 0.9rem;
    color: var(--text-tertiary);
  }

  .auth-footer a {
    color: var(--primary);
    text-decoration: none;
    transition: all 0.3s ease;
  }

  .auth-footer a:hover {
    text-decoration: underline;
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .auth-container {
      grid-template-columns: 1fr;
    }

    .auth-branding {
      padding: var(--space-8);
    }

    .auth-form-container {
      padding: var(--space-8);
    }
  }

  @media (max-width: 768px) {
    .auth-page {
      padding: var(--space-4);
    }

    .auth-container {
      border-radius: 16px;
    }

    .auth-branding {
      display: none;
    }

    .auth-form-container {
      padding: var(--space-6);
    }

    .auth-form-wrapper {
      max-width: 100%;
    }

    .form-header h2 {
      font-size: 1.5rem;
    }

    .brand-stats {
      grid-template-columns: 1fr;
    }
  }
</style>

<footer class="footer">
  <div class="footer-container">
    <div class="footer-section">
      <h4>About DeviceCovers</h4>
      <ul>
        <li><a href="/">Home</a></li>
        <li><a href="/">Features</a></li>
        <li><a href="/">Plans</a></li>
        <li><a href="#">Blog</a></li>
      </ul>
    </div>
    <div class="footer-section">
      <h4>Support</h4>
      <ul>
        <li><a href="#">Help Center</a></li>
        <li><a href="#">Contact Us</a></li>
        <li><a href="#">FAQ</a></li>
        <li><a href="#">Status</a></li>
      </ul>
    </div>
    <div class="footer-section">
      <h4>Legal</h4>
      <ul>
        <li><a href="#">Privacy Policy</a></li>
        <li><a href="#">Terms of Service</a></li>
        <li><a href="#">Security</a></li>
        <li><a href="#">Compliance</a></li>
      </ul>
    </div>
    <div class="footer-section">
      <h4>Connect</h4>
      <ul>
        <li><a href="#">Twitter</a></li>
        <li><a href="#">LinkedIn</a></li>
        <li><a href="#">Facebook</a></li>
        <li><a href="#">Instagram</a></li>
      </ul>
    </div>
  </div>
  <div class="footer-bottom">
    <p>&copy; 2026 DeviceCovers. Enterprise-Grade Premium Laptop Insurance. All rights reserved.</p>
  </div>
</footer>

<script src="/js/main.js"></script>
</body>
</html>
